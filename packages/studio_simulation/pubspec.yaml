name: studio_simulation
description: Stitch playback model for Sewlio Studio simulation preview (pure Dart).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  studio_embroidery: ^0.1.0
  studio_geometry: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
