name: studio_ai
description: Lightweight assistant hooks for Sewlio Studio — proposes Commands, never mutates (pure Dart).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  studio_commands: ^0.1.0
  studio_core: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
