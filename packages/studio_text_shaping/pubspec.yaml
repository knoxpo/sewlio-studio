name: studio_text_shaping
description: HarfBuzz-backed text shaping and glyph-outline extraction for Sewlio Studio (ADR-041).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  ffi: ^2.1.0
  studio_geometry:
    path: ../studio_geometry

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
