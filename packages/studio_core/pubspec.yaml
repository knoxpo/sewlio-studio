name: studio_core
description: Core types for Sewlio Studio — ids, time, service registry (pure Dart, no Flutter).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  studio_diagnostics: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
