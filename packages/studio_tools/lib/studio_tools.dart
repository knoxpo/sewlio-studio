/// Selection and transform tools.
///
/// Tools translate gestures into Commands — they never touch the
/// document directly (ARCH-003).
library;

export 'src/select_tool.dart';
export 'src/selection.dart';
