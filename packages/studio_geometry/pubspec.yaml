name: studio_geometry
description: Geometry model for Sewlio Studio — points, paths, curves, transforms, bounds (pure Dart).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  studio_core: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
