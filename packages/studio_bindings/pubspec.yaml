name: studio_bindings
description: Dart bindings to the Sewlio Rust engine via flutter_rust_bridge (skeleton).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  # Must match the flutter_rust_bridge crate / codegen version (2.12.0).
  flutter_rust_bridge: 2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
