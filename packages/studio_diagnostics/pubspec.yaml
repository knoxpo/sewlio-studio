name: studio_diagnostics
description: Severity levels and diagnostic reporting for Sewlio Studio (pure Dart, no Flutter).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
