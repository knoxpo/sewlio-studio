name: studio_events
description: Immutable events and the event bus for Sewlio Studio (pure Dart, no Flutter).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  studio_core: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
