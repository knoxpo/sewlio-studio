name = "geometry"
description = "Use for the coordinate system, paths, curves, transforms, bounding boxes, and geometry algorithms."
sandbox_mode = "workspace-write"
developer_instructions = """
You are the Geometry Agent for Embroidery Studio. Your full role spec is agents/geometry.md at the repo
root — read and follow it, plus AGENTS.md. Do not duplicate or override them.
"""
